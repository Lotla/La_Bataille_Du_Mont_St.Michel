#include "Commandes.h"
#include <QKeyEvent>
#include <QGraphicsScene>
#include "mes_fourmies.h"
#include "mainwindow.h"
#include <qlistwidget.h>
#include "jeu.h"
#include "base.h"
using namespace std;
#include <iostream>
#include <stdlib.h>
#include <QGraphicsView>
#include <QDebug>
/*
void Commandes::keyPressEvent(QKeyEvent *event)
{
   if (event->key() == Qt::Key_A)
   {
        qDebug()<<"Creation d'une fourmie guerriere";
        spawn_guerriere();
   }
   else if  (event->key() == Qt::Key_Z)
   {
       qDebug()<<"Creation d'une fourmie recolteuse";
   }
   else if  (event->key() == Qt::Key_E)
   {
       qDebug()<<"Creation d'une fourmie chimiste";
   }
   else if  (event->key() == Qt::Key_R)
   {
       qDebug()<<"Creation d'une fourmie super-guerriere";
   }
}
*/
